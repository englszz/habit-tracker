{
  "name": "habit-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site, no build needed'"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}