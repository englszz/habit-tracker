{
  "name": "habit-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site, no build needed'"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}